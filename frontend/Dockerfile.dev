# Dockerfile.dev
FROM node:16

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

# Install any hot-reload tool if needed (e.g., react-scripts or vite)
EXPOSE 80
CMD ["npm", "run", "dev"]
